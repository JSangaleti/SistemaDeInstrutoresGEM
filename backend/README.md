# Backend

## Sobre

Este diretório contém o back-end da aplicação, desenvolvido com Java e Spring Boot.

O projeto já está dockerizado. O banco PostgreSQL e o backend são executados via Docker Compose a partir da **raiz do repositório**.

---

## Como subir o backend com Docker

Na **raiz do projeto**, execute:

```bash
docker compose up --build
```
